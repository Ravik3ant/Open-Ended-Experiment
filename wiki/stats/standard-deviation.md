# Standard Deviation (`s`)

## Definition

Square root of variance; fluctuation scale in original count units.

## Formula

`s = sqrt(s^2)`

## Importance

- Interpretable spread around mean in count units.
- Compared against `sqrt(mu)` to assess Poisson consistency.
